# home server motd
  
this pulls from df/smartctl and uses awk to parse  
  
note: this assumes that drives are /dev/sdX  
if you have m2/nvme drives, this will miss them  

![screenshot](./motd.png)
